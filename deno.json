{
  "tasks": {
    "npm:build": "deno run -A npm.ts"
  },
  "lint": {
    "rules": {
      "exclude": ["prefer-const", "require-yield"]
    },
    "files": {
      "exclude": ["npm/"]
    }
  },
  "fmt": {
    "files": {
      "exclude": ["README.md", "npm/"]
    }
  }
}
